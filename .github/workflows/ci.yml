name: CI

on:
  push:
    branches: [master, main]
  pull_request:
    branches: [master, main]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "8.0.x"

      - name: Restore
        run: dotnet restore KingdomCraft.sln

      - name: Build
        run: dotnet build KingdomCraft.sln --no-restore --configuration Release

      - name: Test
        run: dotnet test KingdomCraft.sln --no-build --configuration Release --verbosity normal
